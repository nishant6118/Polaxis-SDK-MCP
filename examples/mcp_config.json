{
  "_comment": "Example MCP client config for the Polaxis governance proxy. Copy the polaxis block into your MCP client config file (e.g. Claude Desktop: ~/Library/Application Support/Claude/claude_desktop_config.json on Mac, %APPDATA%\\Claude\\claude_desktop_config.json on Windows). Replace YOUR_API_KEY with the key from your Polaxis dashboard.",
  "mcpServers": {
    "polaxis": {
      "command": "polaxis-mcp",
      "env": {
        "POLAXIS_API_KEY": "YOUR_API_KEY_HERE"
      }
    }
  }
}
